global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Host metrics
  - job_name: node
    static_configs:
      - targets: ["${NODE_EXPORTER_TARGET}"]

  # Container metrics
  - job_name: cadvisor
    static_configs:
      - targets: ["${CADVISOR_TARGET}"]
